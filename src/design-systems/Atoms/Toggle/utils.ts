export const toggleContaineClass = ['flex w-fit cursor-pointer items-center justify-center'].join()
export const toggleClass = [
  'flex items-center justify-center font-Poppins text-md bg-transparent   p-[12px] rounded-[50%]',
].join()
